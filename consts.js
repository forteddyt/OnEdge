

const charHeight = 30;
const platformHeight = 10;
const charWidth = 20;
const meteorRad = 10;
const platformWidth = canvas.width*1/2;
const platformX = (canvas.width-platformWidth)/2; //left bound of the platform
const charSpeed = 2;//default movement speed of the player per frame
const meteorSpeed = 2; //default fall speed for meteors per frame
